{
    "variables": {
        "name": "vanilla"
    },
    "builders": [
        {
            "type": "file",
            "content": "{{user `name`}}",
            "target": "{{user `name`}}.txt"
        }
    ]
}
